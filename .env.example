# Express Server Port
PORT=5000

# MongoDB Connection String (Optional - falls back to server/data/enquiries.json if unset or connection fails)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/kidrove
